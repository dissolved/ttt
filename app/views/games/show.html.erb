<%= error_messages_for :game %>
<% content_for :script, javascript_include_tag(:defaults) %>

<div id="game">
  <%= generate_squares(@game) %>
</div>

<%= button_to "New Game?", games_path, :method => :post, :style => 'display: none;', :id => 'NewGameBtn' %>